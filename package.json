{
  "name": "api-telegram-bot",
  "version": "6.0.4",
  "description": "client for telegram bots api",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "tslint --project . && rm -rf dist && tsc && echo 'Build task ok'",
    "docs": "rm -rf docs && typedoc --excludePrivate --mode file --out docs src",
    "prepublishOnly": "tslint --project . && rm -rf dist && tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/felipebergamin/api-telegram-bot.git"
  },
  "keywords": [
    "node",
    "telegram"
  ],
  "author": "Felipe Bergamin <felipebergamin6@gmail.com>",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/felipebergamin/api-telegram-bot/issues"
  },
  "homepage": "https://github.com/felipebergamin/api-telegram-bot#readme",
  "dependencies": {
    "debug": "^2.6.0",
    "node-emoji": "^1.7.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5",
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "@types/debug": "0.0.30",
    "@types/jest": "^24.0.11",
    "@types/node-emoji": "^1.4.0",
    "@types/request-promise-native": "^1.0.15",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.13.1",
    "typedoc": "^0.14.2",
    "typescript": "^3.3.3333"
  }
}
